package sprites;

/**
 * A class represents a clean hall way object.
 * @author Bo Zhao
 */
public class CleanHallway extends Sprite{
	
	/**
	 * Create a new clean hallway object with symbol and
	 * its coordinate (row, column).
	 * 
	 * @param symbol the symbol of a dirt object
	 * @param row the vertical coordinate of this dirt
	 * @param column the horizontal coordinate of this dirt
	 */
	public CleanHallway(char symbol, int, row, int column) {
		super(symbol, row, column);
	}

}
